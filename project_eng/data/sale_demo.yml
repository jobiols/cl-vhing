-
  confirmar venta de la SO0001
-
  !python {model: sale.order, id: sale_order_so0}: |
    self.action_confirm()
-
  confirmar venta de la SO0002
-
  !python {model: sale.order, id: sale_order_so1}: |
    self.action_confirm()
-
  Asignar las tareas de SO001, y del SO002 al res_users2
-
  !python {model: project.task}: |
    tasks = self.search([('project_id.name', 'in', ['IPV: SO0001','IHA: SO0002'])])
    for task in tasks:
      task.user_id = self.env.ref('base.res_users2')
